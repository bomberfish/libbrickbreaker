# libbrickbreaker

A reimplementation of BrickBreaker in C++.

## Build

```bash
cmake -S . -B build
cmake --build build
```

## Output

- Static library: `build/libbrickbreaker.a`

## Public API headers

- `include/libbrickbreaker/libbrickbreaker.hpp` (umbrella include)
